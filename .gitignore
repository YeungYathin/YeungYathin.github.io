others/
.ipynb_checkpoints/
__pycache__/
.DS_Store
# CV source lives in Overleaf; cv/ is a clone of that project (see CLAUDE.md)
/cv/

# Large local archives (e.g. code drops for review) must never enter this Pages repo
*.zip
_site/
.jekyll-cache/
vendor/
.bundle/

# Camera and capture originals: kept on disk, never committed or served
media-src/

# The Clin-JEPA NeurIPS rebuttal: kept on disk as reference, never committed.
# It is the rebuttal for a submission still under review and carries reviewer
# exchanges and unpublished results; this repository is public.
assets/papers/clin-jepa-neurips-2026-rebuttal.md
